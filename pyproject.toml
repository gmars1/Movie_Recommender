[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "your-package-name"
version = "0.1.0"
dependencies = [
    "sqlalchemy>=2.0",
    "pandas>=2.0",
    "openpyxl>=3.0"
]

[tool.setuptools]
packages = ["backend", "FilmsDatabase"]